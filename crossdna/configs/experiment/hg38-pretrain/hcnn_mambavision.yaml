# @package _global_
defaults:
  - /pipeline: bert_hg38
  - override /scheduler: cosine_warmup_timm

model:
  _name_: hcnn_mambavision
  for_representation: false
  alphabet_size: 5  # ATGCN
  out_hidden_dim: 512
  out_dropout: 0.1
  pretrain: true

  # HyperbolicCNN2Image 部分参数
  h2i_kwargs:
    one_hot_dim: 5   # same like alphabet_size
    model_dim: 64    # HyperbolicCNN2Image 中间通道数
    num_layers: 4     # 超曲率卷积层数
    multi_k: false # 是否为每层使用独立的曲率参数
    learnable_k: true  # 曲率 k 是否可学习
    k: 1.0   # 初始曲率值

  # mamba_vision_T_pixel 部分参数
  mamba_vision_kwargs:
    pretrained:        false         # 是否加载预训练权重
    num_seg_classes:   5             # 分割类别数 same like alphabet_size
    depths:            [3, 3, 10, 5] # 四个 stage 的层数
    num_heads:         [2, 4, 8, 16] # 四个 stage 的多头数
    window_size:       [8, 8, 14, 7]  # 窗口划分尺寸
    dim:               128           # patch_embed 输出及第一层通道基数
    in_dim:            64            # patch_embed 中间通道数
    mlp_ratio:         4.0           # MLP 隐藏层比率
    drop_path_rate:    0.2           # 随机深度丢弃率
#    decoder_dim:       null          # 解码器内部维度 (null 表示使用默认)


task:
  # _name_: lm
  _name_: hg38  # equivalent to lm task, plus allows extra metrics to be calculated
  loss: bert_cross_entropy

trainer:
  accelerator: gpu
  devices: 1
  num_nodes: 1
  accumulate_grad_batches: ${div_up:${train.global_batch_size}, ${eval:${trainer.devices} * ${dataset.batch_size} * ${trainer.num_nodes}}}
  max_epochs: 100
  precision: 32  # bf16 only a100
  gradient_clip_val: 1.0
  # strategy: null

dataset:
  batch_size: 65  # Per GPU 128  150
  max_length: 16384 # 262144(2的18次方), 524288(2的19次方) 16384(2 的 14 次方, 也就是16K)  65536是2的16次方
  # optional, default is max_length
  max_length_val: ${dataset.max_length}
  max_length_test: ${dataset.max_length}
  tokenizer_name: char
  use_tokenizer: False
  pad_max_length: null  # needed for bpe tokenizer
  add_eos: true
  rc_aug: false  # 没有使用RC的数据增强。那怎么构造双链呢？？？
  num_workers: 17
  use_fixed_len_val: false  # placing a fixed length val here, but it's really the test
  replace_N_token: false  # replace N (uncertain token) with pad tokens in dataloader
  pad_interval: false  # handle uncertain tokens within the FastaInteral class

scheduler:
  t_in_epochs: False
  t_initial: ${eval:${div_up:${dataset.__train_len}, ${train.global_batch_size}} * ${trainer.max_epochs}}
  warmup_lr_init: 1e-6
  warmup_t: ${eval:${div_up:${dataset.__train_len}, ${train.global_batch_size}} * ${trainer.max_epochs} * 0.01}
  lr_min: ${eval:0.1 * ${optimizer.lr}}

optimizer:
  lr: 1e-3
  weight_decay: 0.0

train:
  gpu_mem: ${eval:"round(float(__import__('subprocess').check_output('nvidia-smi -i 0 --query-gpu=memory.total --format=csv,noheader,nounits', shell=True).strip().decode()) / 1000)"}
  seed: 2222
  global_batch_size: ${eval:${trainer.devices}*${dataset.batch_size}}  # effects the scheduler, need to set properly
